<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="RaisesButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@color/colorRaisedButtonBackground</item>
        <item name="android:textColor">@color/colorRaisedButtonText</item>
    </style>

    <style name="HelpButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@color/colorHelpButtonBackground</item>
        <item name="android:textColor">@color/colorRaisedButtonText</item>
    </style>

    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorDialogText</item>
    </style>


</resources>
